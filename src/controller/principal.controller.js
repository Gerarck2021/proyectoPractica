const indexController = require('./index.controller');
const juegosController = require('./juegos.controller');
const mainController = require('./main.controller');

module.exports = {
    indexController,
    juegosController,
    mainController
}